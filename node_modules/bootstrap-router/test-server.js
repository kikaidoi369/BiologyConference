/**
 * Created by david on 10/12/15.
 */
"use strict";

var express = require('express'),
    path = require('path'),
    app = express(),
    port = 3000,
    bootstrapRouter = require('./index');


app.use('/bootstrap', bootstrapRouter);

// Set error page
app.use(function(req, res){
    res.status(404).send('Not found');
});

module.exports = app;